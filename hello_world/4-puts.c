/**
 * main - Prints "\"Programming is like building a multilingual puzzle"
 * Return: 0
 */

#include <stdio.h>

/**
*The reason for including the header file <stdio.h> in a C program is to provi*de access to the standard input/output library functions. This header file def*ines a number of functions that are commonly used for input and output operati*ons in C programs, including printf, scanf, puts, gets, fopen, fclose, fread, *fwrite, and many others
.*/


int main() {
  
  puts("Programming is like building a multilingual puzzle");
  
  return 0;
  
}
